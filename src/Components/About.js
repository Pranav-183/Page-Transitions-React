import Transitions from "./Transitions"

const About = () => {
   return (
      <>
         <Transitions>
            <h2 className="mt-5 text-center" style={{ color: 'green' }}>These page animations are done with react framer motion</h2>
         </Transitions>
      </>
   )
}

export default About